#include <stdio.h>
/**
 * main - funtion to generate alphabets in reverse
 * section header: Section description)*
 * Return:0 Description of the returned value
 */
int main(void)
{
char c;
for (c = 'z'; c >= 'a'; c++)
putchar(c);
putchar('\n');
return (0);
}

