#include <stdio.h>
/**
 * main - funtion that prints all possible combinations of single-digit numbers.
 * section header: Section description)*
 * Return:0 Description of the returned value
 */
int main(void)
{
int i;
int j;
for (i = 0; i < 9; i++)
putchar(i);
putchar(',');
putchar(j);
putchar('\n');
return (0);
}

